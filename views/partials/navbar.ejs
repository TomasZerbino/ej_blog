<nav class="navbar navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">Hack the blog</a>
    <div class="" id="">
      <% if(req.user){ %>

      <a class="btn btn-primary fw-semibold" href="/admin">Admin</a>
      <a class="btn btn-danger fw-semibold" href="/logout">Logout</a>
      <% }else{ %>
      <a class="btn btn-success fw-semibold" href="/login">Login</a>

      <% } %>
    </div>
  </div>
</nav>
